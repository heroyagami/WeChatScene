# WeChatScene

本仓库维护可复用的微信场景和律师咨询视频生成组件。ChatGPT 负责每日选题、文案和流程控制；GitHub Actions 只做确定性校验与渲染，不在仓库工作流或代码里调用模型。

## 基础规则

- 默认展示完整聊天长页面，使用 Remotion 帧驱动滚动。
- 普通消息左白右绿；曹义德律师固定在左，咨询人固定在右。
- 律师头像固定使用 `public/img/wechat-avatars/caoyide-wechat-avatar.png`。
- 咨询人头像由头像池选择，单条视频内保持不变。
- 普通陈述句通常省略句末句号；标点属于内容编辑，不自动改写用户原文。
- 默认隐藏顶部栏和输入栏。
- 私人素材和每片输入放 `private/` 或 `inputs/`，产物放 `out/`。
- 不提交视频、长图、日志、`generated/` 或 `public/generated/` 等渲染产物。
- 修改后运行 `npm run verify`；影响画面的改动应渲染静帧或成片检查。

## 每日内容结构

每日咨询必须根据当天选题重新创作，不得只替换人名、金额或关键词复用旧稿。

固定顺序：

1. 咨询人右侧连续 3—5 条问题/事实消息，首屏直接交代冲突、关键事实和现实影响。
2. 曹义德律师左侧只回复一条普通文本 `500`。
3. 紧接咨询人右侧 500 元转账卡：`kind:"transfer"`、`transferId:"consultation-fee"`、`transferAmount:"500.00"`、`transferState:"accepted"`、`text:""`。
4. 此后进入完整正式咨询。按具体问题自然覆盖关键事实核实、证据、法律判断条件、止损动作、行动顺序、程序路径和风险边界。
5. 整个 `messages` 数组最后一条必须是曹义德左侧同一笔 500 元收款回执：`role:"left"`、`kind:"transfer"`、`transferId:"consultation-fee"`、`transferAmount:"500.00"`、`transferState:"received"`、`text:""`。

两张转账卡只代表同一笔咨询费，不得新增第二笔付款、第二组卡片或其他收款。

普通消息每条最多 28 字；一句只表达一个意思。消息总数和视频时长不固定，以把问题尽量讲清楚为准。正式咨询要像真实微信问答，不写成长篇法律讲稿，不堆法条，不承诺必然胜诉、固定赔偿、必然退款、必然拘留等确定结果。

## 首屏标题与封面规则

每日输入应提供：

- `coverTag`：法律领域标签，建议 4—6 字，最多 8 字，例如 `婚姻家事`、`劳动纠纷`、`网络侵权`、`校园侵权`。
- `coverTitle`：本期核心问题标题，建议 10—20 字、最多两行，可使用 `\n` 主动换行，例如 `恋爱转账\n怎么防止被追回？`。

视觉规范：

- 首屏只显示“绿色标签 + 白色主标题”，不要“曹义德律师答疑”等副标题，不要大面积黄字、粗黑描边或花哨艺术字。
- 标签使用微信绿圆角胶囊，白字。
- 主标题使用白色粗体，最多两行，轻阴影。
- 标签和标题置于深灰/黑灰半透明圆角底板上，整体专业、克制、清晰。
- 标题应是用户视角的口语化问题，不写成法条标题、论文标题或解释型长标题。

## 运动时间轴规则

当前固定运动逻辑：

1. 视频前 **1.0 秒画面完全静止**，用于展示 `coverTag + coverTitle`。
2. 标题在首秒末尾快速淡出，默认淡出约 0.2 秒。
3. **1.0 秒后开始滚动**。
4. 从开始滚动直到最后一帧，长图必须持续、线性、匀速上移，中途不再停顿。
5. 不允许按单条消息、转账卡或问答节点设置停顿，也不使用分段缓动。
6. `readingCpm` 只用于计算滚动阶段的总时长，不改变“1 秒定帧 + 后续全程匀速”的运动方式。

## 每日生产职责

每天由 ChatGPT 云端任务：

1. 先读取当前 `main` 的 `AGENTS.md`、校验器及 `daily-input/latest.json`。
2. 读取 `content/topic-sources/` 下三份原始选题文档，并结合近期提交历史避免近期重复。
3. 生成当天完整咨询 JSON，并生成对应 `coverTag` 与 `coverTitle`。
4. 写入 `daily-input/latest.json` 并提交 `main`。
5. 提交后立即回读同一路径，验证日期、topic、source、cover、开场 3—5 问、左侧 `500`、accepted 卡、完整咨询、末条 received 回执。
6. 等待 `.github/workflows/render-daily.yml` 完成，并核验 verify、prepare、长图、MP4 和 Artifact。
7. 成功后报告当天选题、完整 commit SHA、实际视频时长、Actions 链接和 Artifact；失败时明确报告失败步骤和可用错误信息。

GitHub Actions 只承担现有确定性校验与渲染。不得把模型调用、选题生成或文案生成放进仓库工作流。


## 发布标题与标签规则

每条新视频必须同时生成便于发布时直接复制的标题与标签：

- `publishTitles`：3—5 个候选标题，数组第 1 个为首选标题。
- 标题应与当天视频事实和法律问题一致，优先使用用户视角、冲突点、行动问题或结果条件；不得标题党、夸大结论或承诺必然结果。
- 每个标题建议 10—30 字，避免与 `coverTitle` 完全机械重复，可以更适合视频号/抖音发布页。
- `publishTags`：5—10 个标签，JSON 中不带 `#`，不得包含空格。优先采用“具体问题标签 + 法律领域标签 + 泛法律标签”的组合。
- 示例：`["遗嘱继承","打印遗嘱","民法典","财产继承","法律咨询","律师普法"]`。
- GitHub Actions 不生成标题或标签，只把 ChatGPT 已写入 `daily-input/latest.json` 的内容确定性转换成发布文案文件。
- prepare 阶段生成 `generated/publish-copy.txt`，Artifact 中打包为 `wechat-YYYY-MM-DD-publish.txt`。
- 发布文案文件必须包含：首选标题、备选标题、带 `#` 的标签行，以及“首选标题 + 标签”的一键复制块。
- 每日云任务验收时必须确认该发布文案文件已进入 Artifact；缺失则不得报告完整成功。

## 音频

BGM 只使用仓库内现有合法配置和已入库音频。每日输入按当前仓库约定使用 `bgm` 与 `bgmVolume`；不得由云任务下载、生成或上传外部音频。
